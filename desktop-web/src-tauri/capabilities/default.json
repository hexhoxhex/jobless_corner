{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions — minimal surface for v1. Add specific allowed-origin / fs / shell entries as features need them.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
