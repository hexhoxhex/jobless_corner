*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/app/build/
/captures
.externalNativeBuild
.cxx
local.properties

# Debug artifacts accumulated at the repo root during testing — not part of
# the source tree. Keep matchers narrow so we don't ignore images/resources
# inside app/src/.
/*.log
/*.png
/*.xml

# OS / editor
Thumbs.db

# Internal handover notes — not part of the public source tree.
/GITHUB_ANON_PUSH.md

# Release artefacts staged for upload — generated, not source.
/dist/

# Diagnostic / probe scratchpad — generated, not source.
/donis-page.html
/stream-test/

# Signing keystores. The real one lives outside the project tree
# (~/.hexhoxhex/release.jks); this is a belt-and-braces guard.
*.jks
*.keystore
